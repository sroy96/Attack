#include <iostream>


/**
 * Node current = head;
Node prev = null;

while(current!=null){
head = current.next;
current.next = prev;
prev = current;
current = head;
}
return prev;
 */

int main()
{
    std::cout<<"Hello, World!"<<std::endl;
    return 0;
}
